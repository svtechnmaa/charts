apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-config
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
data:
  pve.yml: |
{{ include "pve-exporter.pve.yml" . | b64enc | indent 4 }}