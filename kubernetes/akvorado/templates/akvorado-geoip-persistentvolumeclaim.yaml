---
{{- $namespace := .Release.Namespace }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ $namespace }}-{{ .Values.geoip.sharedPersistenceVolume.pvcName }}-pv
  annotations:
    helm.sh/resource-policy: keep
spec:
  accessModes:
    - {{ .Values.geoip.sharedPersistenceVolume.accessModes }}
  capacity:
    storage: {{ .Values.geoip.sharedPersistenceVolume.storageSize }}
  csi:
    driver: seaweedfs-csi-driver
    volumeHandle: "{{ $namespace }}-{{ .Values.geoip.sharedPersistenceVolume.pvcName }}-pv"
    volumeAttributes:
      path: "/{{ $namespace }}/csi/{{ $namespace }}-{{ .Values.geoip.sharedPersistenceVolume.pvcName }}-pv"
  persistentVolumeReclaimPolicy: Retain
  storageClassName: {{ $namespace }}-{{ .Values.geoip.sharedPersistenceVolume.storageClass }}
  
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.geoip.sharedPersistenceVolume.pvcName }}
  annotations:
    helm.sh/resource-policy: keep
spec:
  storageClassName: {{ $namespace }}-{{ .Values.geoip.sharedPersistenceVolume.storageClass }}
  volumeName: {{ $namespace }}-{{ .Values.geoip.sharedPersistenceVolume.pvcName }}-pv
  accessModes:
    - {{ .Values.geoip.sharedPersistenceVolume.accessModes }}
  resources:
    requests:
      storage: {{ .Values.geoip.sharedPersistenceVolume.storageSize }}
