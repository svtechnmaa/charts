------
{{- $basePath := .Values.global.basePath }}
{{- $namespace := .Release.Namespace }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ $namespace }}-inlet-pv
  annotations:
    helm.sh/resource-policy: keep
spec:
  accessModes:
    - {{ .Values.inlet.sharedPersistenceVolume.accessModes }}
  capacity:
    storage: {{ .Values.inlet.sharedPersistenceVolume.storageSize }}
  hostPath:
    path: {{ $basePath }}/{{ $namespace }}/inlet
  persistentVolumeReclaimPolicy: Retain
  storageClassName: {{ $namespace }}-{{ .Values.inlet.sharedPersistenceVolume.storageClass }}
  
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.inlet.sharedPersistenceVolume.pvcName }}
spec:
  storageClassName: {{ $namespace }}-{{ .Values.inlet.sharedPersistenceVolume.storageClass }}
  accessModes:
    - {{ .Values.inlet.sharedPersistenceVolume.accessModes }}
  resources:
    requests:
      storage: {{ .Values.inlet.sharedPersistenceVolume.storageSize }}