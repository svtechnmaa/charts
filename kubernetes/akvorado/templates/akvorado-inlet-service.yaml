apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: {{ .Release.Name }}-inlet
  name: {{ .Release.Name }}-inlet
spec:
  ports:
    - name: "2055"
      port: 2055
      protocol: UDP
      targetPort: 2055
    - name: "6343"
      port: 6343
      protocol: UDP
      targetPort: 6343
    - name: "10179"
      port: 10179
      targetPort: 10179
  selector:
    app.kubernetes.io/component: {{ .Release.Name }}-inlet
  type: {{ .Values.inlet.service.type }}
  {{- if and (eq .Values.inlet.service.type "LoadBalancer") (not (empty .Values.global.backendVip)) }}
  loadBalancerIP: {{ .Values.global.backendVip }}
  {{- end }}
