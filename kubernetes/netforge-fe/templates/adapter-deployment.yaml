{{- if .Values.adapter.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: adapter-manager
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: adapter-manager
spec:
  replicas: {{ .Values.adapter.replicaCount }}
  selector:
    matchLabels:
      {{- include "common.labels.matchLabels" . | nindent 6 }}
      app.kubernetes.io/component: adapter-manager
  template:
    metadata:
      labels:
        {{- include "common.labels.standard" . | nindent 8 }}
        app.kubernetes.io/component: adapter-manager
    spec:
      {{- if .Values.affinity }}
      affinity: {{- include "common.tplvalues.render" (dict "value" .Values.affinity "context" $) | nindent 8 }}
      {{- else }}
      affinity:
        nodeAffinity: {{- include "common.affinities.nodes" (dict "type" .Values.nodeAffinityPreset.type "key" .Values.nodeAffinityPreset.key "values" .Values.nodeAffinityPreset.values) | nindent 10 }}
      {{- end }}
      {{- if .Values.nodeSelector }}
      nodeSelector: {{- include "common.tplvalues.render" (dict "value" .Values.nodeSelector "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.tolerations }}
      tolerations: {{- include "common.tplvalues.render" (dict "value" .Values.tolerations "context" $) | nindent 8 }}
      {{- end }}
      imagePullSecrets:
        - name: ghcr-pull-secret
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: adapter-manager
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ include "common.images.image" (dict "imageRoot" .Values.adapter.image "global" .Values.global) }}
          imagePullPolicy: {{ .Values.adapter.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.adapter.service.targetPort }}
              protocol: TCP
          env:
            - name: TZ
              value: {{ include "common.timezone" (dict "timezone" .Values.timezone "global" .Values.global) }}
            {{- range $key, $value := .Values.adapter.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          envFrom:
            - configMapRef:
                name: {{ .Values.adapter.envFrom.configMap }}
            - secretRef:
                name: {{ .Values.adapter.envFrom.secret }}
          livenessProbe:
            {{- toYaml .Values.adapter.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.adapter.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.adapter.resources | nindent 12 }}
          {{- if .Values.adapter.persistence.enabled }}
          volumeMounts:
            - name: adapters
              mountPath: {{ .Values.adapter.env.ADAPTER_DIR }}
              subPath: {{ .Values.adapter.persistence.subPath }}
              readOnly: true
          {{- end }}
      {{- if .Values.adapter.persistence.enabled }}
      volumes:
        - name: adapters
          persistentVolumeClaim:
            claimName: {{ required "adapter.persistence.existingClaim is required when adapter.persistence.enabled=true" .Values.adapter.persistence.existingClaim }}
      {{- end }}
{{- end }}
