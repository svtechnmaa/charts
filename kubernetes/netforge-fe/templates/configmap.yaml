{{- if .Values.netforgeWebConfig }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: netforge-web-env
data:
  {{- range $key, $val := .Values.netforgeWebConfig }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
{{- end }}