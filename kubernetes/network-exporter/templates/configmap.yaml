apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-configmap
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
data:
  network_exporter.yml: |
{{ include "network-exporter.network-exporter.yml" . | indent 4 }}