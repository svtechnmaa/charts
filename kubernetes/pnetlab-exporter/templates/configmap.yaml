apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-configmap
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
data:
  config.yaml: |
{{ include "pnetlab-exporter.config.yaml" . | indent 4 }}