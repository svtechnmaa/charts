---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "%s-configuration" (include "common.names.fullname" .) }}
  namespace: {{ .Release.Namespace }}
data:
  config.yml: |
{{ .Values.registryConfig | indent 4 }}

