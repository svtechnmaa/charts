apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.pvc.backupDb.claimName }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.pvc.backupDb.size }}
  storageClassName: {{ .Values.pvc.backupDb.storageClass }}