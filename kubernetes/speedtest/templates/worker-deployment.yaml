apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "common.names.fullname" . }}-worker
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: worker
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" (dict "value" .Values.commonAnnotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ include "common.replicas" (dict "replicaCount" .Values.replicaCounts.worker "global" .Values.global) }}
  selector:
    matchLabels:
      {{- include "common.labels.matchLabels" . | nindent 6 }}
      app.kubernetes.io/component: worker
  template:
    metadata:
      labels:
        {{- include "common.labels.standard" . | nindent 8 }}
        app.kubernetes.io/component: worker
        {{- if .Values.commonLabels }}
        {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 8 }}
        {{- end }}
        {{- with .Values.podLabels }}
        {{- include "common.tplvalues.render" (dict "value" . "context" $) | nindent 8 }}
        {{- end }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- include "common.tplvalues.render" (dict "value" . "context" $) | nindent 8 }}
      {{- end }}
    spec:
      containers:
        - name: worker
          image: {{ include "common.images.image" (dict "imageRoot" .Values.image.worker "global" .Values.global) }}
          env:
            - name: NUM_WORKERS
              value: {{ .Values.worker.numWorkers | quote }}
            - name: MQTT_BROKER_HOST
              value: {{ .Values.mqtt.brokerHost | quote }}
            - name: MQTT_BROKER_PORT
              value: {{ .Values.mqtt.brokerPort | quote }}
            - name: MQTT_BROKER_PATH
              value: {{ .Values.mqtt.brokerPath | quote }}
            - name: MQTT_BROKER_USERNAME
              value: {{ .Values.mqtt.username | quote }}
            - name: MQTT_BROKER_PASSWORD
              value: {{ .Values.mqtt.password | quote }}
            - name: REDIS_HOST
              value: {{ .Values.redis.host | quote }}
            - name: REDIS_PORT
              value: {{ .Values.redis.port | quote }}
            - name: REDIS_DB
              value: "0"
            - name: DB_ENGINE
              value: {{ .Values.django.dbEngine | quote }}
            - name: MAX_CONCURRENT_PER_SERVER
              value: {{ .Values.mqtt.maxConcurrency | quote }}
          envFrom:
            - secretRef:
                name: django-db-secret
          volumeMounts:
            - name: logs-volume
              mountPath: "/app/logs"
      volumes:
        - name: logs-volume
          persistentVolumeClaim:
            claimName: {{ .Values.pvc.mqttLogs.claimName }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
