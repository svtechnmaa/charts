apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.pvc.media.claimName }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.pvc.media.size }}
  storageClassName: {{ .Values.pvc.media.storageClass }}