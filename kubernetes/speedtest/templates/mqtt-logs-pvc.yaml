apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.pvc.mqttLogs.claimName }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.pvc.mqttLogs.size }}
  storageClassName: {{ .Values.pvc.mqttLogs.storageClass }}