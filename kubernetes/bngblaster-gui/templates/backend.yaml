apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "bngblaster-gui.backendName" . }}
  labels:
    {{- include "bngblaster-gui.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  replicas: {{ .Values.backend.replicaCount }}
  selector:
    matchLabels:
      {{- include "bngblaster-gui.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: backend
  template:
    metadata:
      labels:
{{- include "bngblaster-gui.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: backend
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      initContainers:
        - name: init-backend-data
          image: {{ include "bngblaster-gui.image" (dict "root" . "component" "backend" "image" .Values.backend.image) | quote }}
          imagePullPolicy: {{ .Values.backend.image.pullPolicy }}
          command:
            - /bin/sh
            - -c
            - |
              set -eu
              mkdir -p /mnt/backend-data
              seed_source="{{ .Values.sharedVolume.path }}/all_conf.yml"
              seed_dest="/mnt/backend-data/all_conf.yml"
              if [ -f "$seed_dest" ]; then
                echo "Data already initialized, skipping"
              elif [ -f "$seed_source" ]; then
                cp "$seed_source" "$seed_dest"
                echo "Initialized data successfully"
              else
                echo "Seed file $seed_source not found; backend data volume was left empty"
              fi
          volumeMounts:
            - name: {{ .Values.sharedVolume.volumeName }}
              mountPath: /mnt/backend-data
      containers:
        - name: backend
          image: {{ include "bngblaster-gui.image" (dict "root" . "component" "backend" "image" .Values.backend.image) | quote }}
          imagePullPolicy: {{ .Values.backend.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8000
          envFrom:
            - configMapRef:
                name: {{ include "bngblaster-gui.fullname" . }}-config
            - secretRef:
                name: {{ include "bngblaster-gui.fullname" . }}-secret
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
          volumeMounts:
            - name: {{ .Values.sharedVolume.volumeName }}
              mountPath: {{ .Values.sharedVolume.path }}
          resources:
            {{- toYaml .Values.backend.resources | nindent 12 }}
      volumes:
        - name: {{ .Values.sharedVolume.volumeName }}
          persistentVolumeClaim:
            claimName: {{ printf "%s-%s" (include "bngblaster-gui.fullname" .) .Values.sharedVolume.pvcName | trunc 63 | trimSuffix "-" }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "bngblaster-gui.backendName" . }}
  labels:
    {{- include "bngblaster-gui.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: {{ .Values.backend.service.targetPort }}
  selector:
    {{- include "bngblaster-gui.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: backend
