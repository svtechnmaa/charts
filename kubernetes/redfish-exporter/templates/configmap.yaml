apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-configmap
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
data:
{{- range $name, $block := .Values.config }}
  {{ $name }}.yml: |
{{ include "redfish-exporter.config.yml" $block | indent 4 }}
{{- end }}