apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-configmap
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
data:
  esx.yml: |
{{ include "vmware-exporter.esx.yml" . | indent 4 }}